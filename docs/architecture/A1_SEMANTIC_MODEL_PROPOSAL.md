# A1 · Semantic Model — PROPOSED

> ## ⚠️ PROPOSED — 결정이 아니다
>
> | | |
> |---|---|
> | **Status** | **PROPOSED** |
> | **Architecture Frozen** | **NO** |
> | **Implementation Authorized** | **NO** |
> | **Phase** | A1a — Architecture Decision Packet |
> | **Product Version** | v0.1 MVP |
> | **Product SSOT** | `JuQode-Private/docs/current/00_MASTER_INDEX.md` |
> | **판정 대장** | `JuQode-Private/docs/current/14_A1_ARCHITECTURE_DECISION_PACKET.md` |
> | **확인일** | 2026-09-06 (A1a) |
>
> **이 문서는 초안이다.** 여기 있는 어떤 문장도 Canon 이 아니고, 구현 지시가 아니다.
> 선택지 · 비용 · 반대 근거는 위의 판정 대장에 있다. **결정은 PM 과 Founder 가 한다.**

---

## 1. 풀어야 하는 긴장

Canon 은 자리(Semantic Place)를 이렇게 못박았다.

> **테두리가 없다 · 셀 수 없다 · 떼어낼 수 없다 · 개체가 아니다. 재료의 성질이다.**

그러나 저장은 반대를 요구한다.

- Problem 은 자리에 매여야 한다 (MUST #6)
- 이름 정정은 **재스캔에도 살아남아야** 한다
- Evidence 와 Deep View 가 자리를 참조한다

**저장하려면 안정된 동일성이 필요하다.** 이것이 A1 이 풀어야 하는 구조적 긴장이다.

---

## 2. 제안 — 안이 아니라 밖에서 개체가 아니다

| | |
|---|---|
| **내부** | `place_id` 는 존재한다. 단 **좌표도 순번도 아닌 `Semantic Source Mapping` 의 서명**에서 파생된다 |
| **사용자** | 자리를 만들 수도 지울 수도 옮길 수도 없다. **목록으로 열거되지 않는다.** 이름은 집혔을 때만 온다 |
| **재스캔** | 매핑 서명이 유지되면 같은 자리다 → 이름 정정 · Problem · Evidence 가 살아남는다 |
| **겹침** | 자리 ↔ 소스는 **N:N**. 한 파일이 두 자리에 속할 수 있다. **분할이 아니다** |

> **이것을 정하지 않으면 `Place` 테이블을 쓰는 순간 조용히 Feature Map 으로 되돌아간다.**

---

## 3. 자리는 어떻게 나오는가 (PROPOSED)

```
진입점 · 라우트 후보  ──씨앗──┐
매니페스트 · 의존 그래프 ─────┼──▶ 후보 덩어리 ──▶ AI 라벨 ──▶ 사람 정정
Git 변경 동시성 ─────────────┘         │
                                      └─▶ 7개 초과 · 확신 낮음 → 『모르는 곳』
```

| 규칙 | 근거 |
|---|---|
| 파일 빈도는 자리를 만들지 못한다 | 실측 — 한 저장소는 파일의 99%가 로그였다 |
| 폴더는 자리가 아니다 | 폴더는 분할이고, 자리는 겹친다 |
| 자리 7개를 넘기지 않는다 | 3R.5 수용량 한계 (또렷이 구분되는 방향 5~7개) |
| 뜻이 없는 곳은 `모르는 곳` | P3 — 발명하지 않는다 |

---

## 4. Realm 독립

자리 · 질량 · 상태 · 증거는 **Realm 에 묶이지 않는다.**
v0.1 은 Field/Grain 하나만 쓰지만, 모델에는 Realm 어휘가 들어가지 않는다.

---

## 5. 미결

- **재스캔 안정성**을 실제 저장소에서 측정하지 않았다 — 서명이 코드 변경을 얼마나 견디는지 모른다
- 자리가 20개인 소프트웨어는 3R.5 이래 여전히 미해결이다
- 자리 이름을 **고칠 수 있다는 것을 화면이 어떻게 알리는가** 는 상시 라벨 금지와 충돌할 수 있다 → D1/QA
