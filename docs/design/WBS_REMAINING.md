# 남은 작업 WBS — MVP 인증 → V1

> **rev 2 · 2026-09-13** — Owner/PM 판정(조건부 승인) 반영. 판정 전문은 **`SPEC_RECONCILE.md`**.
> rev 1 대비 바뀐 것 여섯: ① X-1 여섯 건 전부 판정됨 ② 마이그레이션 4=X-6 · 5=P1-5 확정
> ③ **M-3 도그푸드가 R 트랙보다 먼저** ④ Windows 인증은 **10 1809+ 와 11 둘 다**
> ⑤ 실행이 **Phase A → MVP 인증 → V1** 로 나뉘고 **P1~P4 는 보류**
> ⑥ **P1-2 는 낡은 3표 설계에서 시작할 수 없다**
>
> 이어받은 것: `WBS_IMPLEMENTATION.md` 의 R · P0~P4 **ID 와 정의는 그대로**다.
> 근거: `RESUME.md` · `WBS-LEDGER.md` · `DEFERRED_VALIDATION.md` · `ERD.md` · `DBML_REVIEW.md`
> + 2026-09-13 실측(단위 597/597 · `check-dbml` 통과 · Supabase `list_tables`).

---

## 0. 시작 상태 (2026-09-13 실측)

| | 값 | 확인 방법 |
|---|---|---|
| 단위 테스트 | **597 / 597** | `npm run test:unit` (40.5s) |
| e2e | 3종 · 마지막 측정 배치 36 | **미재측정 — Phase A ①** |
| WBS 39 | 37 IMPLEMENTED · 2 NOT STARTED | `WBS-LEDGER.md` |
| 로컬 DB | 26표 + 마이그레이션 3 | `schema.sql` · `db.js` |
| 서버 DB | **없음** — 프로토타입 잔재 20표만 | `list_tables(xkydfvbwgwcoenbpzdew)` |
| ERD 그림 | 49표 · 관계 49 · 그룹 11 | `check-dbml.mjs` → 통과 |
| v0.2 (R·P0~P4) | **0 / 38 착수** | 산출물 파일 전부 부재 |
| **MVP 인증** | **NO** | WBS-32 · WBS-33 미완 · Windows 0/2 환경 |

---

## 1. 이 계획의 목표는 V1 이 아니다

```
        지금 ──▶ Phase A ──▶ Phase B ──▶ Phase C ──▶ ★ MVP_CERTIFIED = YES ──▶ Phase D (V1)
              게이트 없음    사람       교정·인증                              ← 여기까지 보류
```

**MVP_CERTIFIED = YES 의 정의** (넷 전부여야 한다)

1. **WBS-32 닫힘** — 비개발자 도그푸드가 실제로 돌았고 G-1~G-11 판정이 있다
2. **WBS-33 닫힘** — 패키징 · 서명 표시가 대상 OS 에서 확인됐다
3. **`MVP_WINDOWS_CERTIFIED`** — **Windows 10 1809+ 와 Windows 11 둘 다** 실행·증거
4. **R 트랙 완료 + 재측정** — `readability-after.json` 이 before 대비 개선을 숫자로 보이고,
   바뀐 렌더러의 뮤테이션 생존자가 0 (또는 각각에 이유)

**V1(P1~P4) 프로덕션 구현은 그 뒤다.** 예외 하나뿐: **X-6** — PM 이 「기존 MVP 프로세스
재조정 진실성 결함의 수정」이라고 확인하면 MVP 인증 전에 진행할 수 있다 (게이트 **G-F**).

---

## 2. 게이트 — 일곱

| ID | 게이트 | 막는 것 | 무엇이 있어야 열리나 | 지금 |
|---|---|---|---|---|
| **G-A** | 비개발자 테스터 | **M-3** · R-10 · P4-4 | M-3/R-10 에 3명(같은 코호트 가능) · P4-4 에 **다른** 5명 | 닫힘 |
| **G-B** | Windows **두** 환경 | M-1·M-2·M-6 · P4-1·P4-2 · DV 13행 | **Win10 1809+ AND Win11.** VM 가능. 한 대로는 `STARTED` 까지만 | 닫힘 |
| **G-C** | Canon 판정 (Owner) | R-6 · P2-4 · P2-6 · X-5 · X-6 | `JuQode-Private` 의 `18`/`15`/`20` 개정. 이 저장소는 요청서만 낸다 | 닫힘 |
| **G-D** | Supabase 승격 | P1-1 → P1 전체 | D-A7 확정됨. **V1 보류로 실행이 뒤로 밀렸다** | 결정 · 미실행 |
| **G-E** | 결제사 선정 | `payment`·`billing_webhook` | V2. 지금 열 필요 없다 (D-A5) | 의도적 |
| **G-F** | **X-6 을 MVP 전에?** | X-6 (마이그레이션 4) | PM 한 줄: 재조정 진실성 결함 수정이 인증에 필요한가 | **신규 · 대기** |
| **G-G** | **판정→컬럼 해석 확인** | X-7 → P1-2 | `SPEC_RECONCILE.md` §2 의 확인 ①②③ | **신규 · 대기** |

**변하지 않은 규칙:** 문구는 `copy.js` 에서 먼저 고치지 않는다. `18`/`15` 가 먼저다 (G-C).

---

## 3. 트랙 X — 설계 정합 (계획 작업 · 보류 대상 아님)

> 판정문: *"Planning/reconciliation work for X may continue."*
> **X-1 은 판정됨.** 남은 것은 그 판정을 문서와 그림에 옮기는 일이다.

### X-1 · 판정 기록 — ✅ **완료 (2026-09-13)**
- **산출물** `docs/design/SPEC_RECONCILE.md` — 여섯 건 판정 + 컬럼 해석 3건 + 마이그레이션 번호 + 나머지 판정 넷
- **딸린 정정** `BACKEND_A.md`(§3 낡음 배너) · `WBS_IMPLEMENTATION.md`(대체 배너) · `ERD.md`(§2 번호 · §3 판정 주의)

### X-7 · 판정을 ERD 에 반영 **← 신규 · Phase A**
- **세부** `erd.dbml` 의 V1 절반을 판정에 맞춘다. 빼는 것: `policy` 의 `narrative_engine_override`·`allow_file_edit`·`allow_terminal`·`allow_deploy` · `plan` 의 `narrative_engine`·`monthly_narrative_calls`·`allow_terminal` · `usage_day` 의 `qc_runs`·`narrative_calls` · 룩업 `engine`. 남기는 것: `policy.daily_work_limit_override` · `audit_usage.kind` 셋 · `audit_kind` 룩업 셋.
- **선행** X-1 · **게이트** **G-G** (해석 확인 먼저)
- **파일범위** `docs/design/erd.dbml` · `docs/design/ERD.md` §3 · `docs/design/DBDIAGRAM.md`(표 수)
- **소요** 120분
- **완료 기준** `node docs/design/check-dbml.mjs` 통과 · 표 수가 49 → **약 45** 로 줄고 그 수가 세 문서에서 같다 · 남은 V1 표 어디에도 "아무도 읽지 않는 컬럼" 이 없다.
- **오류 시** `check-dbml.mjs` — 컬럼을 빼면 그 컬럼을 쓰던 `Ref:` 와 `Note` 가 같이 깨진다

### X-3 · `erd.dbml` ↔ `schema.sql` 자동 대조 검사기 **← Phase A**
- **세부** 지금 `check-dbml.mjs` 는 **dbdiagram 문법**만 본다. 그림이 실제 스키마와 같은지는 아무도 재지 않는다 — `DBDIAGRAM.md` 가 "이 그림은 사본입니다" 라고 적어 둔 그대로다. MVP 26표의 **표 이름 · 컬럼 이름 · not null** 을 `schema.sql` + `db.js MIGRATIONS` 에서 뽑아 대조한다.
- **선행** 없음 · **게이트** 없음 (V1 절반은 대조 대상 아님 → X-7 과 독립)
- **파일범위** `docs/design/check-dbml.mjs` **확장** — 새 파일 만들지 않는다
- **소요** 90분
- **완료 기준** `schema.sql` 에서 컬럼 하나를 지우면 검사기가 **빨개진다**(직접 지워 확인). V1·`제안` 표는 대조 대상에서 **명시적으로** 제외된다.
- **오류 시** `schema.sql` 은 Postgres 방언, `db.js` 마이그레이션은 SQLite — 파서가 둘 다 읽어야 한다

### X-4 · dbdiagram 제출 링크 실물 확인
- **선행** X-3 · **X-7** · **게이트** 사람(계정, 30초)
- **파일범위** `docs/design/DBDIAGRAM.md`(링크 기록)
- **소요** 15분 · **완료 기준** 링크를 열면 갱신된 표가 그룹별로 보인다

### X-5 · DBML 리뷰 잔여 4건 → Canon 요청서
- **세부** 리뷰 6건 중 #3·#1·#6(그림) · #5 blob 누수(W-27b) · #2 테스트 고정(W-26b)은 **처리됨.** 남은 넷:

| 건 | 내용 | 권고 | 단계 |
|---|---|---|---|
| **#4a** | `quick_command_run` 에 pid 없음 → 앱 재시작 시 **도는 개발 서버를 못 찾고 무조건 "확인 불가"** | **고친다 — 실제 버그** | X-6 · G-F |
| #4b | `work` 의 pid 가 `work_signal` JSON 안에 | 고친다(같은 마이그레이션) | X-6 |
| #5b | 죽은 컬럼 `output_ref`·`stream_ref` | **살리거나 지운다** | X-6 에 동승 |
| #2 | 교차참조 복합 FK | **기록만.** 테스트가 이미 고정, 전례도 있다 | 요청서에만 |

- **선행** 없음 · **게이트** G-C
- **파일범위** `docs/design/CANON_CHANGE_REQUEST.md` (= **P0-3 과 같은 문서**)
- **소요** 45분

### X-6 · 마이그레이션 **4** — 프로세스 추적 ⚠️ **G-F 대기**
- **세부** `app_session` 표 + `work`·`quick_command_run` 에 `runner_session_id`·`runner_pid`·`runner_started_at`. ERD 에 이미 `제안` 으로 그려져 있다. PID 재사용 방어는 `main.js` 의 기존 규칙(프로세스 시작 시각이 work 시작보다 앞설 때만 신뢰)을 그대로 쓴다.
- **선행** X-5 · **게이트** **G-F**(MVP 전 진행 승인) · **G-C**(`schema.sql` 은 Canon 축자 사본)
- **파일범위** `app/main/db/db.js` `MIGRATIONS` · `app/main/db/repo.js`(`processFor`·`reconcileQcRuns`) · `app/main/main.js` · `app/main/qc/run.js` · `tests/store.test.js` · `tests/qc.test.js` · `docs/design/erd.dbml`
- **소요** 150분
- **완료 기준** 앱을 껐다 켜도 **실제로 도는 개발 서버가 `running` 으로 남는다**(죽은 것은 여전히 `unknown`). `tests/store.test.js:35` 의 26표 단언이 27표로 바뀐다.
- **왜 MVP 인증에 걸릴 수 있나** 이건 제품이 **사실이 아닌 것을 화면에 말하는** 결함이다 — 도그푸드에서 사용자가 부딪힐 수 있고, `13` §11 DoD 의 "이해할 수 있는가" 에 직접 닿는다. **판단은 PM 의 것이다.**

### X-2 · `consume_work()` 재작성 사양 🛑 **V1 보류**
- **세부** X-1 판정을 RPC 하나의 SQL 로 확정. 현재 `BACKEND_A.md` §3 의 RPC 는 **버려진다** — 멱등 키도, 장치 검증도, `plan→policy` 순서도 없다.
- **선행** X-7 · **게이트** G-G · **단계** Phase D
- **파일범위** `docs/design/BACKEND_A.md` §3 · §4② (마이그레이션 번호 3→5 정정)
- **완료 기준** RPC 본문이 다섯을 전부 적는다: ① `auth.uid()` 만 쓰고 클라이언트 `user_id` 를 읽지 않음 ② `audit_usage` 행을 먼저 만들고 UNIQUE 충돌이면 **올리지 않고 기존 결과를 돌려줌**(X-1c) ③ **장치 소유 검증 — 남의 계정 장치는 거부**(X-1d) ④ `plan → policy.daily_work_limit_override` 순서 ⑤ `for update` 유지. **`works` 하나만 센다**(X-1b).

**X 합계: 7작업** (X-1 완료 · X-3·X-7 은 Phase A · X-6 은 G-F · X-2·X-4 는 뒤)

---

## 4. 트랙 M — MVP 인증

### M-3 · WBS-32 도그푸드 **← 순서 승격: R 트랙보다 먼저**
- **세부** `09` §14 시나리오 G-1~G-11. 실제 프로젝트에서. **고쳐 주지 않고 어디서 막히는지만 적는다.**
- **선행** 없음 (지금 빌드로 가능) · **게이트** **G-A** · **단계** Phase B
- **파일범위** `docs/dev-evidence/mvp-run/WBS-32-DOGFOOD.md`(신규)
- **소요** 90분 × 3명
- **완료 기준** G-1~G-11 각각 통과/막힘 + 막힌 지점 한 문장. **비개발자의 실제 오해가 R 트랙의 1차 입력이 된다** — R 의 근거가 코드 계수에서 사람의 말로 바뀐다.
- **코호트** 이 참가자들이 **고치기 전 R-10 코호트를 겸한다.** **P4-4 는 다른 사람** — 고친 뒤 · 설치본이고, 같은 사람은 두 번째에 이미 알고 있다.

### M-1 · Windows **두** 환경 확보
- **세부** **Windows 10 1809+ 와 Windows 11**. VM 허용 — 대상 OS 의 진짜 동작을 내놓는다면. ConPTY · `PATHEXT` · NTFS 대소문자는 VM 에서도 진짜다.
- **선행** 없음 · **게이트** **G-B**
- **완료 기준** **두 환경 각각에서** `git clone` + `npm ci` + `npm start` 가 된다.
- **⚠** 한 환경만 확보되면 `WINDOWS_VALIDATION_STARTED` 까지다. **인증이 아니다.**

### M-2 · `verify-windows.ps1` — 환경 ① (Win10 1809+)
### M-6 · `verify-windows.ps1` — 환경 ② (Win11) **← 신규**
- **세부** 각 환경에서 **독립 실행.** DV-1~10 · DV-12~16: 부팅 ×5 · 시작 시간 · NSIS 설치 · 패키징 exe 실행 · PE 인증서 · ConPTY · 프로세스 트리/`taskkill /T` · 테마 6조합 · `claude.cmd` 탐지 · NTFS 대소문자 1행 · e2e 3종.
- **선행** M-1 · **게이트** G-B · **단계** Phase C
- **파일범위** `scripts/verify-windows.ps1` · `scripts/windows-spikes.mjs` · `scripts/windows-cdp-probe.mjs` · `docs/dev-evidence/mvp-run/DEFERRED_VALIDATION.md`
- **소요** 각 120분
- **완료 기준** DV 13행이 **환경별로** `PASS` 또는 구체적 실패. DV-14·DV-15 는 `UNRESOLVED_NEEDS_RERUN` 이므로 여기서 원인이 나와야 한다.
- **🛑 절대 규칙** 한 호스트의 결과를 두 환경으로 보고하지 않는다. 한 환경만 통과하면 그 DV 행은 여전히 `IMPLEMENTED_PENDING_VALIDATION` 이고 **어느 환경이 통과했는지 옆에 적는다.**

### M-2a · 파이프 셸 Windows 갈래 실구현 (M-2/M-6 이 실패시킬 때만)
- **세부** cmd.exe 또는 PowerShell 용 종료코드 마커. `%ERRORLEVEL%` / `$LASTEXITCODE`. **차단 목록은 만들지 않는다** (`19` §C4 · `tests/term.test.js` 가 검사한다).
- **선행** M-2 · **게이트** G-B (측정도 Windows 에서 · **두 환경 다**)
- **파일범위** `app/main/term/session.js` `shellFor()` · `tests/term.test.js`
- **소요** 120분 + 재측정 ×2
- **⚠ 예상 확률 높음** `term/session.js:57` 의 Windows 갈래는 **자리표시**다. `$?`/`printf` 마커는 cmd.exe 에서 안 돈다.

### M-4 · WBS-33 닫기 (패키징 · 서명)
- **세부** M-2 · M-6 이 DV-3·4·5·16 을 **둘 다** 닫으면 WBS-33 이 닫힌다. **P4-1 · P4-2 와 같은 작업이다 — 두 번 하지 않는다.**
- **선행** M-2 · M-6 · **게이트** G-B
- **파일범위** `docs/dev-evidence/mvp-run/WBS-LEDGER.md`

### M-7 · **MVP 인증 선언** **← 신규**
- **세부** §1 의 넷을 하나씩 증거로 확인하고 적는다. 하나라도 빠지면 **NO** 다.
- **선행** M-3 · M-4 · R-11 · **게이트** G-A · G-B
- **파일범위** `docs/dev-evidence/mvp-run/MVP-CERTIFICATION.md`(신규) · `RESUME.md` · `README.md`(상태 표)
- **완료 기준** 넷 각각에 증거 파일명. **`MVP_WINDOWS_CERTIFIED` 는 두 환경 로그가 각각 있을 때만 YES.**
- **이 항목이 Phase D 의 문을 연다.**

**M 합계: 7작업** (M-2a 는 조건부)

---

## 5. 트랙 R — 가독성 (M-3 뒤)

> **순서 변경:** R-1 의 기준선 측정 전에 **M-3 도그푸드가 끝나 있어야 한다.**
> R-2~R-9 의 우선순위를 코드 계수가 아니라 **사람이 막힌 지점**이 정한다.

### R-0 · e2e 영향 범위 먼저 재기 **← Phase A (M-3 을 기다리지 않는다)**
- **세부** R-2~R-5 는 `sc03.js`·`sc04.js` 를 구조적으로 바꾼다. `visual.mjs` 는 **4047줄 · 단언 618개**이고 `sc03` 57회 · `sc04` 36회 · `reader` 86회 참조한다. **어떤 단언이 레이아웃에 묶여 있고 어떤 것이 규칙에 묶여 있는지** 분류한다.
- **선행** 없음 · **게이트** 없음
- **파일범위** `tests/e2e/visual.mjs`(읽기만) · `docs/dev-evidence/readability-assertion-map.md`
- **소요** 60분
- **완료 기준** 618개 중 R 로 깨질 것의 개수와 목록. 각각 「고쳐야 할 단언」/「지워야 할 단언」/「그대로」.
- **왜 필요한가** RESUME §6: *"검사도 개정을 따라간다."* **검사는 카드의 벽을 금지하는 것만큼 쉽게 강제한다.** 분류 없이 R-2 를 시작하면 빨간 단언을 하나씩 만나며 즉흥적으로 지우게 되고, 그때 지운 것이 규칙이었는지 레이아웃이었는지 나중에 알 수 없다.

### R-1 · 기준선 측정 — **M-3 뒤**
실측으로 확인한 시작값(2026-09-13):

| 지표 | 값 |
|---|---|
| `pri` 버튼 | SC-01 **1** · SC-02 **1** · SC-03 **7** · SC-04 **1** · TD-01 **2** |
| ghost 버튼 | SC-02 **11** · SC-03 **9** · SC-04 **10** · TD-01 **11** · SC-01 **0** |
| `copy.js` 키 | **396** |
| 화면 코드 | `sc02.js` 733 · `sc03.js` 678 · `td01.js` 643 · `sc04.js` 437 |

- **선행** **M-3** · R-0 · **완료 기준** `docs/dev-evidence/readability-before.json` 에 5화면 × 4지표 + **M-3 이 지목한 지점**

### R-2 ~ R-9 — 기존 정의 그대로, 우선순위는 M-3 이 정한다
- **R-2 정정** "5화면 각각 정확히 1개" 인데 **TD-01 은 2개**다. SC-03 7→1 만이 아니라 TD-01 2→1 도 범위다.
- **R-6 · R-7 은 문구가 바뀐다 → G-C 뒤.** R-6(각주 접기)이 지우지 않고 **접는** 것이라 `18` 개정 없이 가능한지 P0-3 에서 함께 묻는다.

### R-10 · 재측정 + 비개발자 3명
- **코호트 정정** **고치기 전** 관찰은 **M-3 이 이미 했다.** R-10 에 남는 것은 **재측정 + 고친 뒤 확인**이다.
- **선행** R-2~R-9 · **게이트** G-A
- **완료 기준** `readability-after.json` 이 before 대비 **빈 공간 감소 · 컨트롤 감소 · 첫 화면 카드 증가**를 숫자로 보인다. **M-3 에서 막혔던 지점이 막히지 않는지**를 같은 사람에게 확인한다.

### R-11 · 렌더러 뮤테이션 재스윕
- **세부** 배치 35 가 렌더러 **생존 0** 을 달성했다. R-2~R-5 가 그 코드를 다시 쓰면 **그 측정은 무효가 된다.**
- **선행** R-2~R-5 · R-10 · **게이트** 없음
- **파일범위** `scripts/mutate/sweep-renderer.py` 실행 · `docs/dev-evidence/mvp-run/BATCH-R-sweep.json`
- **소요** 실행 2~4h(무인) + 사살 작업 별도
- **주의** `rm -rf .mutate-work` 먼저 · **스윕 중 대상 파일 편집 금지** · 타임아웃은 가짜 kill(`E2E_TIMEOUT`) · GONE ≠ kill

**R 합계: 12작업 · 약 15시간**

---

## 6. 트랙 P0 — 화면 확인

- **P0-1** (대조표) — **Phase A.** 게이트 없음. `docs/design/SCREEN_MATRIX.md`
- **P0-2** (로그인 · 상한 카드 와이어) — Phase C. 선행 P0-1
- **P0-3 정정 — 4건이 아니라 8건이다.** 기존 4건(CF-22 · CF-23 · F-17 · write-class) + ⑤ X-5 #4 프로세스 컬럼 ⑥ #5b 죽은 컬럼 ⑦ #2 교차참조(기록만) ⑧ R-6 각주 접기가 `18` 개정을 요구하는가. P2-4·P2-6 문구도 **미리 넣는다** — 한 번의 판정으로 묶는 편이 싸다.
  - **파일범위** `docs/design/CANON_CHANGE_REQUEST.md` (X-5 와 **같은 문서**)
  - **게이트** 이 문서가 **G-C 를 여는 재료**다

**P0 합계: 3작업 · 약 3h 30m**

---

## 7. 트랙 P1 · P2 · P3 · P4 — 🛑 **V1 보류**

> 판정문: *"do not execute broad P1 / P2 / P3 / P4 production implementation yet,
> except an MVP correctness fix that is independently required for certification."*
> **M-7 이 YES 를 내기 전에는 착수하지 않는다.** 아래는 판정에 맞춰 정정만 해 둔다.

### P1 — DB (8작업 · 약 8h)
- **P1-1** 프로토타입 정리 — 실측 삭제 대상 **20표**(`night_run_v0` 10 + `public` 미러 10). 게이트 G-D
- **P1-2 🛑 「서버 테이블 3개」는 낡았다.** X-1a 가 **ERD 모델**로 확정했고 X-7 이 그것을 약 19표로 줄인다. **선행 = X-7 · X-2** · 게이트 G-G. **낡은 3표 텍스트에서 시작하지 않는다.**
- **P1-3 정정** 동시성 + **멱등** + **장치 거부** 셋을 잰다:
  - 상한 3 → `true,true,true,false,false` · 병렬 20 → 정확히 상한에서 멈춤 (기존)
  - 같은 `(device_id, kind, client_ref)` 2회 → `usage_day.works` 가 **1만** 오른다 (X-1c)
  - 남의 계정 `device_id` → **거부** (X-1d)
- **P1-5 정정** 마이그레이션 **5** (4 는 X-6)
- **P1-6** 가장 위험한 항목 — 빠뜨리면 계정 전환 시 남의 프로젝트가 보인다. `grep -n "from project" app/main/` 전수
- **P1-7 정정** 등급 판정에서 **서술 관련 칸은 읽지 않는다** (X-1f)

### P2 — CRUD (7작업 · 약 8h)
- **P2-1 파일범위** `app/main/security.js:45` 의 `local` 판정 한 줄. 카운터는 **지우지 않는다** — e2e 의 주장이 "외부 요청 0" 에서 "허용된 하나 외 0" 으로 **문장만** 바뀐다
- **P2-3** preload 채널 26개 ↔ `visual.mjs` surface 목록. 두 곳을 같이 고친다
- **P2-4 · P2-6** G-C 대기 (문구)
- **P2-5 정정 — 소모 지점은 한 곳이다.** X-1b 가 `works` 하나로 확정했다. `supervisor.preflight` 4단계 직후, 그곳뿐. (rev 1 의 "3곳" 은 판정으로 철회됐다.) 다만 `audit_usage` 는 `kind` 로 qc·narrative 도 **기록**할 수 있다 — 상한이 아니라 관측이다

### P3 — 테스트 (7작업 · 약 7h 30m)
- **P3-6** 멱등·장치 경계 회귀 — RPC 에서 멱등 검사를 지우면 **빨개진다**(직접 지워 확인)
- **P3-7** `check-dbml.mjs` 를 `npm test` 경로에 — **X-3 직후로 앞당길 수 있다**(15분, 게이트 없음). Phase A 에 붙이는 것을 권한다

### P4 — 배포 (5작업 · 약 10h 45m)
- **P4-1 · P4-2 = M-2 · M-6 · M-4 와 같은 작업.** 두 번 하지 않는다
- **P4-4** 5명 파일럿 — **M-3/R-10 과 다른 코호트** (고친 뒤 · 설치본)
- **P4-3** `get_advisors(security)` 가 빈 것이 완료 기준

---

## 8. 의존 그래프

```
PHASE A — 게이트 없음 · 지금
  ① npm run test:e2e (기준선)
  ② X-1 ✅ 완료 → SPEC_RECONCILE.md
  ③ X-3 (대조 검사기) ──▶ P3-7 (npm test 에 붙이기)
  ④ R-0 (단언 지도)
  ⑤ P0-1 (대조표) ──▶ P0-2 ─▶ P0-3 ──[G-C 요청]──▶ …
  ⑥ X-7 (ERD 반영) ◀──[G-G]──  ──▶ X-4

PHASE B — [G-A]
  M-3 도그푸드 ────────────┐
                           │ (사람의 말이 R 의 입력)
PHASE C — MVP 교정 · 인증   ▼
  R-1 ─┬─▶ R-2 ─┬─▶ R-6* ──┐
       ├─▶ R-3 ─▶ R-4 ─▶ R-5│
       ├─▶ R-7           ├──▶ R-10 ─▶ R-11 ──┐
       ├─▶ R-8 ◀── R-2    │                   │
       └─▶ R-9 ───────────┘                   │
  X-5 ──[G-C]──▶ X-6* (마이그레이션 4) ─[G-F]─┤
  M-1 ─▶ M-2 (Win10) ─┬─▶ M-2a (실패 시)      │
       └─▶ M-6 (Win11)┴─▶ M-4 = WBS-33 ───────┤
  P3-4 · P3-5 (전체 스위트 · 변이) ────────────┤
                                              ▼
                                    ★ M-7  MVP_CERTIFIED
                                              │
PHASE D — V1 (보류 해제)                       ▼
  X-2 ─▶ P1-1[G-D] ─▶ P1-2[G-G] ─┬─▶ P1-3 ─┬─▶ P1-4 ─▶ P4-3
                                  │         └─▶ P1-7 ─▶ P1-8
                                  ├─▶ P1-5(마 5) ─▶ P1-6 ─▶ P3-1
                                  └─▶ P2-1 ─▶ P2-2 ─▶ P2-3 ─┬─▶ P2-4* ─▶ P3-3
                                                             └─▶ P2-5 ─┬─▶ P2-6*
                                                                       ├─▶ P2-7
                                                                       ├─▶ P3-2
                                                                       └─▶ P3-6
  P3-* ─▶ P4-4 ─▶ P4-5

*  = G-C(Canon) 뒤   |   X-6 = G-F 뒤
```

---

## 9. 임계 경로 — 넷

| # | 경로 | 지배 요인 | 소요 |
|---|---|---|---|
| **1** | `M-3 → R-1 → R-2/3/4/5 → R-10 → R-11` | **G-A 사람** · R-11 무인 스윕 | 사람 대기 + 약 15h |
| **2** | `M-1 → M-2(Win10) → M-6(Win11) → M-2a? → M-4` | **G-B 두 환경** · M-2a 확률 높음 | 기계 대기 + 4~7h |
| **3** | `X-3 → P3-7` · `X-7 → X-4` · `P0-1 → P0-3 → G-C` | 게이트 없음 · **판정 요청을 여는 재료** | 약 6h |
| **4** | `M-7 → X-2 → P1-1 → P1-2 → P2-* → P3-*` | **★ MVP 인증이 문이다** | 인증 후 약 32h |

**경로 1·2·3 은 서로 막지 않는다 — 병렬이다.** 경로 4 는 M-7 전에 시작하지 않는다.
**MVP 인증까지의 임계 경로는 1 과 2 중 긴 쪽이고, 둘 다 사람/기계 대기가 지배한다.**

---

## 10. 합계

| 트랙 | 작업 | 에이전트 시간 | 단계 | 막는 게이트 |
|---|---|---|---|---|
| **X** 설계 정합 | 7 (1 완료) | 9h | A / C / D | G-G · G-F · G-C |
| **M** MVP 인증 | 7 | 10h 30m | B / C | **G-A · G-B** |
| **R** 가독성 | 12 | 15h | A(R-0) / C | G-A · G-C |
| **P0** 화면 확인 | 3 | 3h 30m | A / C | — (G-C 를 연다) |
| **P1** DB | 8 | 8h | **D 보류** | G-D · G-G |
| **P2** CRUD | 7 | 8h | **D 보류** | G-C |
| **P3** 테스트 | 7 | 7h 30m | A(P3-7) / C / D | — |
| **P4** 배포 | 5 | 10h 45m | **D 보류** | G-B · G-A |
| **계** | **56** | **약 72h** | | |

**단계별**

| 단계 | 작업 | 에이전트 시간 | 문 |
|---|---|---|---|
| **Phase A** (지금) | **7** | **약 7h** | 없음 |
| **Phase B** | 1 | 4h 30m(3명) | G-A |
| **Phase C** | 21 | 약 28h | G-A · G-B · G-C · G-F |
| ★ **M-7 인증** | — | — | 위 셋 전부 |
| **Phase D** (V1) | 27 | 약 32h | M-7 · G-D · G-G |

rev 1 은 52작업 · 62h 였다. 늘어난 4작업/10h: **M-6**(Win11 별도 실행) · **M-7**(인증 선언) ·
**X-7**(판정을 ERD 에 반영) · X-1 이 완료로 바뀌며 X-2 가 Phase D 로 이동.

---

## 11. 이 계획이 **하지 않는** 것

| 항목 | 왜 |
|---|---|
| 결제 연동 | D-A5 — `payment`·`billing_webhook` 은 선언만 |
| **등급별 서술층 차단** | **X-1f — 서버 등급이 사용자 자기 Claude 를 가르지 않는다.** 호스팅 추론은 별도 결정 |
| **qc·narrative 별도 상한** | **X-1b — 제품의 활성 하루 상한은 `works` 하나** |
| **`policy` 의 투기적 컬럼 넷** | **X-1e — 아무도 읽지 않는 컬럼을 프로덕션 스키마에 넣지 않는다** |
| 조직/RBAC · SSO | D-A4 |
| 자체 관리자 화면 | D-A2 — Supabase Studio 가 V1 관리자 화면 |
| 교차참조 복합 FK | X-5 #2 — 테스트가 고정(W-26b), 기록만 |
| 공통 파일 관리 표 | 관리할 파일이 사실상 diff 하나 |
| Figma 이전 | 파일이 비어 있고 MCP 한도 소진. 캔버스 25장이 대체한다 |
| node-pty | 능력 격리 검사가 금지 · 검증 불가 OS 에 네이티브 모듈을 들이지 않는다 |

---

## 12. 첫 실행 배치 — Phase A (지금 시작 가능)

| 순 | 작업 | 게이트 | 소요 | 완료 기준 |
|---|---|---|---|---|
| 1 | `npm run test:e2e` 기준선 | 없음 | 30m | 3종 초록. **빨간 스위트 위에 R-1 기준선을 세우지 않는다** |
| 2 | X-1 판정 기록 | — | ✅ | `SPEC_RECONCILE.md` |
| 3 | **X-3** ERD↔스키마 대조 검사기 | 없음 | 90m | `schema.sql` 컬럼 하나 지우면 빨개진다 |
| 4 | **P3-7** 검사기를 `npm test` 에 | 없음 | 15m | `npm run test:unit` 이 검사기를 돈다 |
| 5 | **R-0** 단언 영향 지도 | 없음 | 60m | 618 중 깨질 것의 목록 · 3분류 |
| 6 | **P0-1** 화면↔Canon 대조표 | 없음 | 60m | `SCREEN_MATRIX.md` 행이 전부 채워짐 |
| 7 | **X-7** 판정을 ERD 에 반영 | **G-G** | 120m | `check-dbml.mjs` 통과 · 표 수가 세 문서에서 같다 |

**약 6시간 15분.** 7번만 G-G(확인 ①②③) 대기이고 나머지 여섯은 지금 바로 된다.

**Phase A 가 끝나면 무엇이 열리나:** P0-1 → P0-2 → P0-3 이 **G-C 요청서**를 만들고,
X-7 → X-4 가 ERD 제출을 갱신하고, R-0 이 **Phase C 의 R 작업을 안전하게** 만든다.
**사람이 오면(G-A) 즉시 Phase B(M-3)로 넘어간다 — R 작업보다 먼저.**

---

## 13. 알고 있는 위험 다섯

1. **R 트랙이 e2e 618 단언을 흔든다.** R-0 없이 시작하면 빨간 단언을 즉흥적으로 지우게 되고, 그때 지운 것이 규칙이었는지 레이아웃이었는지 나중에 알 수 없다.
2. **M-2/M-6 이 파이프 셸 Windows 갈래를 실패시킬 확률이 높다.** `$?`/`printf` 마커는 cmd.exe 에서 안 돈다 — M-2a 예산을 미리 잡아 둔다.
3. **한 Windows 환경의 결과를 둘로 보고하는 것** — 이 런이 가장 경계해 온 실패 유형이다. `MVP_WINDOWS_CERTIFIED` 는 로그가 **둘** 일 때만 YES.
4. **P1-6 을 빠뜨리면 계정 전환 시 남의 프로젝트가 보인다.** `grep` 전수 + P3-1 이 그것을 실패시킬 수 있어야 한다. (Phase D)
5. **X-7 을 건너뛰고 P1-2 를 시작하면** 판정이 폐기한 컬럼을 프로덕션 스키마에 넣게 된다. G-G 가 그것을 막는다.
